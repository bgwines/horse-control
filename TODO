* refs -- prefixes, branches, tags, etc...
* aliases
* checkout -> "load" ?
* be able to execute commands from any subdirectory of the root of the repository
* dupes in staging area
* input validation
* diff stats
* stage, then mod without committing (remember to update docs for loadUnstagedFiles)