* refs -- prefixes, branches, tags, etc...
* input validation
* branches
    * cherry-pick
    * merging
    * "current" branch
* revert
* -q -v
* writerT
* can't initialize a repo in a subdirectory of another repo